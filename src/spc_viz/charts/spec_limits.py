"""Spec-limit drawing helpers.

Reserved for shared USL / LSL / nominal-line and tolerance-band drawing
logic. The original ``chart_utils`` module inlined this logic in each
chart builder rather than sharing helpers; this module is left as a
placeholder so future shared spec-limit code has a clear home without
having to refactor a chart builder first.
"""
