"""Chart export helpers (PNG via kaleido, etc.).

Reserved for chart export logic. The original ``chart_utils`` module did
not contain any kaleido / PNG export code — that lives in
``shared_ui.py`` for now. This module is kept as an empty placeholder
so future export helpers have a clear home.
"""
